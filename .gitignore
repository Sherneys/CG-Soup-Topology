# Python
__pycache__/
*.pyc

# Local Claude Code settings
.claude/

# LaTeX build intermediates (main.pdf is committed on purpose)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper2/*.aux
paper2/*.bbl
paper2/*.blg
paper2/*.brf
paper2/*.log
paper2/*.out
paper2/*.toc
paper2/*.xdv

# Local tool binaries (tectonic.exe lives here; not for the repo)
tools/

# Round-4 matrix figure: per-cell render intermediates (regenerate via
# scripts/make_matrix_figure.py; only the assembled matrix*.png ship)
paper2/figures/matrix_cells/
